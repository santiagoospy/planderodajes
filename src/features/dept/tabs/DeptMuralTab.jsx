// TODO: migrate from index_legacy.html
export default function DeptMuralTab({ color, deptKey, projectId, project, isAdmin }) {
  return (
    <div style={{ padding:'16px 0', textAlign:'center', color:'var(--text-muted)', fontSize:13, fontFamily:'inherit' }}>
      Archivos y fotos — en migración
    </div>
  )
}
